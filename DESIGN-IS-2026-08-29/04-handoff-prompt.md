# Implementation handoff

```text
/make-plan Refine the Keelhaularr interface in /Users/cbrooker/Code/keelhaularr using DESIGN-IS-2026-08-29/00-scope.md, 01-evidence.md, 02-scorecard.md, and 03-verdict.md as the source of truth. Preserve all existing backend behavior, deletion safety, qBittorrent recovery behavior, dirty-worktree changes, and the parchment/teal/gold/coral nautical identity. Implement a cleaner, more efficient UI with these concrete outcomes: (1) compress the dashboard hero so the manifest appears materially earlier on desktop and mobile; provide exactly one primary scan action; remove the redundant lower standing-orders panel; hide result-only filters and batch controls before the first scan; retain a compact summary; distinguish a truly clear source from a filter yielding no matches; make mobile connection statuses visibly named; (2) add a Settings section navigator that keeps one focused group visible at a time—Connections, Cleanup rules, Automation, Account, and System—with Connections first during onboarding, all values retained in the same form state, and one global save action; separate qBittorrent connection controls from automatic-replacement controls without changing their persisted shape; (3) simplify Operations navigation and poll only data needed by the active view, while keeping active jobs fresh; (4) keep nautical voice in branding but use literal operational labels, including Username, Password, Sign in, Scan Radarr & Sonarr, Untracked files, Size limits, Remove & search again, Both apps, App, Quarantine, Delete permanently, Remove exclusion, and Automatically replace slow or stalled downloads; map raw statuses to human labels; (5) change “Ready” to “Configured” unless a live result exists; show “No matching files” when filters hide results; disclose that running scheduled maintenance can apply configured quarantine retention and display purgedQuarantineCount when returned; (6) complete accessible interaction behavior: full tab/current-state semantics, shared visible focus styles, dialog initial focus/focus containment/Escape/focus return, background isolation, a skip link, corrected normal-text contrast, and larger compact mobile targets; (7) consolidate spacing/type/color usage and keep the responsive layout free of body-level horizontal overflow. Add focused frontend tests where the current project setup supports them; otherwise verify with TypeScript/Vite build, server tests, diff checks, and browser interaction at desktop and 390px mobile. Do not alter unrelated user changes and do not commit.
```
